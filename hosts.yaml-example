all:
  vars:
    iqube_ssh_private_key: ~/.ssh/iqube
    iqube_ssh_public_key: ~/.ssh/iqube.pub
  children:
    iqube_admin:
      hosts:
        admin1:

    iqube_nfs_server:
      hosts:
        admin1:

    iqube_controller:
      hosts:
        ctl[1:3]:

    iqube_compute:
      hosts:
        c[1:4]:
